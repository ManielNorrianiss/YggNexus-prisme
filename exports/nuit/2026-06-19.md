# Rapport de nuit — YggNexus — 2026-06-19

> Mode TRAVAIL DE NUIT AUTONOME. Emperor a tisse pendant que Josue dormait.
> Aucune question posee, aucun bouton. Decisions prises seul, notees ci-dessous.

## TL;DR

Les deux taches de la nuit sont **faites et verifiees**, sans toucher a ta machine
(ni Supabase, ni Ollama, ni git push) :

1. `prisme/publish.py` revalide maintenant aussi **`/best/<cat>`** et **`/categories/<cat>`**
   (+ l'index `/categories`), en plus de `/` et `/tools/<slug>`. Fini le cache perime
   sur les pages de regroupement apres une (re)classification.
2. Nouveau dossier **`prisme/tests/`** : 39 tests purs (sans reseau), **39/39 verts**.

Rien de casse. `py_compile` OK sur tout le Python touche. Frontend pas touche (donc
pas de `tsc` a faire).

---

## 1. publish.py — revalidation des pages de regroupement

### Le probleme (rappel de la memoire)
`publish.py` ne revalidait que `/` et `/tools/<slug>`. Les pages `/best/<cat>` et
`/categories/<cat>` ont `revalidate = 86400` (24 h) cote Next.js. Apres une
reclassification, elles montraient une vieille photo jusqu'a 24 h (ou redeploy manuel).

### Ce que j'ai change
Dans `main()` de `publish.py` :
- `paths` demarre maintenant a `["/", "/categories"]` et on accumule un set `cat_slugs`.
- Dans la boucle d'upsert, chaque categorie d'un outil est ajoutee a `cat_slugs`.
- Apres la boucle, on lit **toutes** les categories existantes dans Supabase
  (`select slug from categories`) et on les fusionne dans `cat_slugs`, puis on ajoute
  `/best/<slug>` et `/categories/<slug>` aux `paths` a revalider.
- Un print resume le nombre de chemins (dont X categories).

### Decision (et pourquoi)
**J'ai choisi de revalider TOUTES les categories existantes, pas seulement celles des
outils publies dans le run.** Raison : couvrir le cas du **retrait** de categorie. Quand
un outil sort d'une categorie (reclassification), il ne figure plus dans la liste de
cette categorie — mais sa vieille page de regroupement doit quand meme etre rafraichie.
Se limiter aux categories "touchees" raterait ce cas. Les categories sont peu nombreuses
(7), donc le cout est negligeable. La lecture Supabase est enrobee d'un `try/except` :
si elle echoue, on retombe proprement sur les seules categories touchees.

C'est une lecture seule de plus cote Supabase (un `select slug`), aucun risque d'ecriture.

### Verification
- `python -m py_compile publish.py` -> **OK**.
- Revue du diff : 3 inserts cibles, ancres uniques verifiees par assert avant ecriture
  (regle "montage Windows tronque" respectee : edite via bash/python, pas Edit/Write).

---

## 2. prisme/tests/ — 39 tests purs, 39/39 verts

Nouveau dossier `prisme/tests/` :
- `conftest.py` : met `prisme/` sur `sys.path` (pytest marche depuis la racine ou prisme/).
- `test_export_tools.py` (13 tests) : filet **score /10** (echelle 100->10, clamp 0..10,
  None, non-numerique) et filet **SEO** (seo_title/seo_description derives + tronques
  quand le LLM ne les fournit pas, conserves quand fournis). `staging.get_tool_categories`
  est neutralise (monkeypatch) -> aucun acces a staging.db.
- `test_b6_seo.py` (12 tests) : **content_hash** (deterministe, insensible a l'ordre des
  outils, change si outil ajoute / categorie differente, inclut PROMPT_VERSION) et
  **garde-fou anti-mince** `validate_category_content` (seuil MIN_DESC, troncatures
  MAX_DESC/MAX_TITLE/MAX_SEO_DESC, fallbacks titre/description).
- `test_b4_classification.py` (14 tests) : **validate_classification** (clamp confidence
  haut/bas/non-numerique, primary invalide->None->unclassified, seuil, secondary filtre
  /dedup/max 2/non-liste, defauts surs) + `taxonomy_slugs` et `content_hash_for`.
- `README.md` : comment lancer.
- `check_no_pytest.py` : harnais de secours stdlib (voir decision ci-dessous).

### Decision (et pourquoi) — verification sans pytest
**pytest n'a PAS pu etre installe dans le sandbox** : PyPI y est bloque (proxy 403),
exactement comme sur ta machine Windows. Pour quand meme **prouver cette nuit** que les
tests passent, j'ai ecrit `tests/check_no_pytest.py` : un harnais en stdlib pur qui
injecte un stub minimal de `pytest` (decorateur `fixture` no-op), applique a la main le
seul monkeypatch necessaire, et execute toutes les fonctions `test_*`.

Resultat : **39 passes, 0 echec.** Les fichiers de test restent du **pytest idiomatique**
(chez toi, `pytest` les ramassera tels quels). `check_no_pytest.py` n'est qu'un filet de
verif, pas un fichier de test (pytest ne le collecte pas).

### Sortie de la verif
```
Resultat: 39 passes, 0 echecs.
```
(detail PASS par test dans le log de la session ; `py_compile tests/*.py` aussi OK.)

---

## RUNBOOK du matin (commandes a lancer par TOI, dans l'ordre)

> Tout est local au repo **prisme** (`D:\Emperor\YggNexus`). Frontend non touche.
> Rien d'irreversible ici ; le gros (publish/SQL) reste sous ton controle.

### A. Menage d'un residu que je n'ai pas pu effacer
```
del D:\Emperor\YggNexus\prisme\publish.py.bak
```
*Pourquoi :* j'avais fait une copie de sauvegarde avant d'editer ; je l'ai videe (0 octet)
mais le montage m'interdit de la supprimer. Efface-la pour garder un `git status` propre.

### B. Verifier le code (optionnel mais conseille)
```
cd D:\Emperor\YggNexus\prisme
python -m py_compile publish.py
pip install pytest        :: si pas deja installe
pytest                    :: doit afficher 39 passed
```
*Pourquoi :* confirmer chez toi ce que j'ai confirme dans le sandbox. Si `pip` est bloque,
`python tests\check_no_pytest.py` fait la meme verif sans pytest.

### C. Regarder l'etat git AVANT de committer
```
cd D:\Emperor\YggNexus
git status
```
*Pourquoi :* il y a une bizarrerie **pre-existante** (pas de moi) dans l'index :
`prisme/staging.py`, `prisme/run_nightly.py`, `prisme/seed_raw.py`, `prisme/run_nightly.cmd`
apparaissent a la fois "deleted (staged)" et "untracked". Ca ressemble a un artefact d'un
commit fait depuis le sandbox (cf. memoire montage). Verifie que ces fichiers existent bien
sur le disque, puis `git add` chacun pour reconcilier l'index. **Ne push pas avant** d'avoir
verifie que rien d'utile n'est marque "deleted".

### D. Committer + pousser MON travail (repo prisme)
```
cd D:\Emperor\YggNexus
git add prisme/publish.py prisme/tests
git commit -m "B7: revalide /best/<cat> et /categories/<cat>; tests purs B4/B6/export (39)"
git push origin main
```
*Pourquoi :* publie le code. (Pas de push frontend : rien change cote site.)

### E. Activer la revalidation amelioree (au prochain publish, automatique)
```
cd D:\Emperor\YggNexus\prisme
python export_tools.py        :: regenere data/tools.json depuis staging.db
python publish.py             :: upsert Supabase + revalide / , /tools/* , /best/* , /categories/*
```
*Pourquoi :* le nouveau comportement s'applique des le prochain `publish.py`. Il appellera
ton endpoint `/api/revalidate` avec les pages de regroupement en plus. Verifie le print
"X chemin(s) a revalider (dont N categorie(s) -> /best + /categories)".
*Pre-requis :* `REVALIDATE_SECRET` et `SITE_URL` doivent etre dans `prisme/.env` (deja le cas
pour tes publishs precedents).

### F. SQL Supabase / redeploy Vercel
**Rien a faire.** Cette nuit ne touche ni le schema ni le frontend. Pas de migration SQL,
pas de redeploy. (La revalidation on-demand remplace justement le redeploy manuel pour
rafraichir /best et /categories.)

### G. Verifier en live (apres un publish)
Charger `https://www.yggnexus.com/best/<cat>?v=1` et `/categories/<cat>?v=1` (le `?v=`
contourne le cache navigateur) pour confirmer que la page reflete le dernier etat.

---

## Reste a faire / backlog (inchange, pour memoire)
- **B6 phase 2** : prose des pages "X vs Y" et "alternatives" (necessite une table de
  stockage cote schema + rendu frontend).
- **staging.db fragile** : `PRAGMA integrity_check` a faire sur Windows ; ne pas lire
  staging.db depuis le sandbox (ressort "malformed").
- Grossir le catalogue (nouvelles sources, relancer B1->B5) ; automatiser la chaine de
  nuit + recherche semantique.

## Sur quoi j'ai bloque
- **Demarrage** : la session avait monte `D:\ElderBrain` au lieu de `D:\Emperor`. Resolu
  apres que tu m'aies redonne l'acces a `D:\Emperor` (le bon dossier).
- **pytest non installable dans le sandbox** (PyPI 403) -> contourne avec le harnais stdlib
  (voir decision #2). Aucun impact sur la qualite des tests.
- **`publish.py.bak` non supprimable** depuis le sandbox (permissions montage) -> vide a
  0 octet, a `del` au matin (etape A).
- **Bizarrerie git pre-existante** sur quelques fichiers prisme (etape C) -> signalee, pas
  touchee (corriger l'index depuis le sandbox aurait pu laisser des verrous .git).

Farevel. Le terrain est net : code valide, tests verts, rien de casse, et tout ce qui
demande ta machine est dans le RUNBOOK.
